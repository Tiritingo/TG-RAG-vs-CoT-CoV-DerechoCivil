# =============================================================================
# TG-RAG-vs-CoT-CoV-DerechoCivil — dependencias
#
#   pip install -r requirements.txt
#
# El pipeline de indexacion y evaluacion se ejecuta en Google Colab con GPU T4
# (el modelo multilingual-e5-large necesita GPU: en CPU, embeber los ~63.000
# chunks pasa de 3 horas). Los scripts de 05_Codigo_Fuente/SCRAPERS corren en
# local sin GPU.
#
# DEPENDENCIA EXTERNA NO INSTALABLE CON PIP
#   Tesseract OCR con idioma espanol, para ocr_corte_suprema.py
#   Windows : https://github.com/UB-Mannheim/tesseract/wiki
#             (marcar "Spanish" en Additional language data)
#   Linux   : sudo apt install tesseract-ocr tesseract-ocr-spa
#   macOS   : brew install tesseract tesseract-lang
#   Verificar: tesseract --list-langs   debe listar 'spa'
#
#   LibreOffice, para extraer_texto_cc.py (convierte los 15 .doc de Word 97)
#   https://es.libreoffice.org/descarga/
# =============================================================================

# ---------- orquestacion RAG y agente ----------
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.0
langchain-google-genai>=2.0.0
langchain-groq>=0.2.0
langgraph>=0.2.0

# ---------- vectorstore y embeddings ----------
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ---------- extraccion de documentos ----------
pymupdf>=1.24.0          # rasterizado y capa de texto; sin dependencias externas
pypdf>=4.0.0
striprtf>=0.0.26
python-docx>=1.1.0
beautifulsoup4>=4.12.0

# ---------- OCR ----------
pytesseract>=0.3.10      # requiere el binario de Tesseract, ver arriba
pillow>=10.0.0

# ---------- descarga de corpus ----------
requests>=2.31.0
playwright>=1.40.0       # solo para los scrapers originales

# ---------- analisis y estadistica ----------
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0            # Wilcoxon, Spearman
scikit-learn>=1.3.0      # kappa ponderado
matplotlib>=3.7.0

# ---------- utilidades ----------
tqdm>=4.66.0
openpyxl>=3.1.0
